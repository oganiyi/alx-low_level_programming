#include "main.h"

/**
 * add - function to add two numbers
 * @a: first parameter
 * @b: secod parameter
 * Return: return the addition of the parameters
*/

int add(int a, int b)
{
	return (a + b);
}
