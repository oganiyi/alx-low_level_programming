#include <stdio.h>
#include "main.h"

/**
 * mul - function to multiply to multiply two numbers
 * @a: first number
 * @b: seconf number
 * Return: the product of a and b
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
